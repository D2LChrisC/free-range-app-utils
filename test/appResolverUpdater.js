'use strict';

var updater = require('../src/appResolverUpdater');

describe( 'app resolver updater', function() {
	describe( 'update version', function() {
		it( 'pwns', function() {

		} );
	} );

	beforeEach( function() {
		jasmine.Ajax.install();
	} );

	afterEach( function() {
		jasmine.Ajax.uninstall();
	} );
} );
